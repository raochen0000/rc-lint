{
  "name": "rc-lint-config",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "publish": "lerna publish",
    "prepare": "husky install "
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
